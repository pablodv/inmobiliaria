<div class="secondary-navigation">
  <ul class="wat-cf">
    <li class="first <%= klass == 'list' ? 'active' : '' %>">
    	<%= link_to "List", tenants_path  %>
  	</li>
    <li class="<%= klass == 'new' ? 'active' : '' %>">
    	<%= link_to "New", new_tenant_path %>
  	</li>
  	<% if klass == 'edit' %>
	    <li class="active">
	    	<%= link_to "Edit", edit_tenant_path(tenant) %>
	  	</li>
  	<% end %>
  	<% if klass == 'show' %>
	  	<li class="active">
	  		<%= link_to "Show", tenant_path(tenant) %>
	  	</li>
  	<% end %>
  </ul>
</div>